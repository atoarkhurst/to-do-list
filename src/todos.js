
let createTodo( title, description, dueDate, priority ) {
    // Create empty object
    const todo = {
        title,
        description,
        dueDate,
        priority,
    };
    
    return todo;
    
}